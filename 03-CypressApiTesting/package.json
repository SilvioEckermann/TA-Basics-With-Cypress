{
  "scripts": {
    "test": "npx cypress run",
    "testDeleteArticle": "npx cypress run --spec cypress/e2e/deleteArticle.cy.js"
  },
  "devDependencies": {
    "cypress": "^13.0.0",
    "cypress-mochawesome-reporter": "^3.5.1"
  }
}
